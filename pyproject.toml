[project]
name = "nautilus-zerodte"
version = "0.1.0"
description = "0DTE options extension for NautilusTrader: gates, greek policy, venue adapters (Deribit, IB), backtest/paper parity."
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "nautilus-trader[ib]==1.229.0",
    "pydantic>=2.13.4",
    "pyyaml>=6.0.3",
    "typer>=0.26.7",
]

[project.scripts]
nautilus-zerodte = "nautilus_zerodte.cli.main:app"

[dependency-groups]
dev = [
    "pytest>=8.4.0",
    "ruff>=0.12.0",
]

[tool.uv]
prerelease = "allow"
# Security floor: protobuf < 5.29.6 is vulnerable to a JSON recursion depth
# bypass (GHSA-7gcm-g887-7qv7). protobuf reaches us transitively via the
# nautilus-trader "ib" extra, so pin the floor here rather than relying on
# whatever that extra happens to resolve to.
constraint-dependencies = ["protobuf>=5.29.6"]

[tool.uv.sources]
nautilus-trader = { index = "nautech" }

[[tool.uv.index]]
name = "nautech"
url = "https://packages.nautechsystems.io/simple"
explicit = true

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/nautilus_zerodte"]

[tool.hatch.build.targets.sdist]
include = ["src/nautilus_zerodte"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]

[tool.ruff]
target-version = "py314"
line-length = 100
src = ["src", "tests"]

[tool.ruff.lint]
select = ["E", "F", "I", "UP"]
